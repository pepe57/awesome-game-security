Armcall 是一个仅头文件的 C++20 库，用于在 ARM64 上直接发起 Windows 内核系统调用。它会解析 ntdll 导出，从每条 SVC 指令中提取立即数操作数，并动态生成可执行存根，通过 svc 调用并返回，而无需经过反作弊或 EDR 软件可能挂钩的导入 thunk。AC_SYSCALL 宏为调用 NT 函数（如内存分配和系统时间查询）提供了简单接口，并支持可插拔的标准库抽象以适配自定义环境。它适用于游戏安全研究、反作弊规避研究，以及 ARM64 平台上的 Windows 底层逆向工程。
